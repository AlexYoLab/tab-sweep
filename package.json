{
  "name": "tab-sweep",
  "version": "1.0.0",
  "description": "A beautiful Chrome extension to manage and clean your tabs",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:ext": "vite build && cp -r dist/* extension/dist/"
  },
  "dependencies": {
    "pinia": "^2.1.7",
    "vue": "^3.4.21"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.4",
    "canvas": "^3.2.3",
    "vite": "^5.2.0"
  }
}
